# 2026-05-19

## Today's outcome
Slice 8b close-out: CLAUDE.md edit, smoke-test PR (4 verification 
cases), branch protection. Then [Slice 9 — Playwright E2E / something 
else, depending on energy].

## Decisions made
- (capture as the day unfolds)

## Stuck on
- Nothing yet

## Tomorrow's first task
- (fill in at end of day)

## CI smoke test
Branch created to verify CI workflow runs on PRs and catches the
right failure classes.

## PHASE 0 CLOSED — all 9 acceptance criteria met

Slice 9 (Playwright E2E + CI integration) merged. Both CI jobs
(ci + e2e) green and required on main.

Phase 0 took 6 working days: scaffold → RLS multi-tenancy → auth →
onboarding → invitations → observability → CI → E2E.

What exists now:
- Multi-tenant SaaS with database-enforced isolation (Postgres RLS +
  FORCE RLS, withTenant helper, 4 SECURITY DEFINER functions)
- Bearer-token auth (argon2, SHA-256 session hashing)
- Full owner onboarding (5 steps) + admin invitation flow
- Sentry + PostHog wiring (no-DSN-no-op contract)
- 117 backend tests + 1 Playwright E2E, all green in CI
- CI on every PR: lint + typecheck + test + build + e2e, branch
  protected

Bugs surfaced by E2E, logged for pre-pilot fix:
- Zod .optional() ordering bug in branding + invite forms (empty
  strings rejected before .optional() rescues) — onboarding funnel
  leak, fix before pilot demo

Carried forward (deferred.md highlights):
- Windows Turbo crash (hidden bugs in slices 6, 7, 8b — now the
  highest-leverage post-Phase-0 fix)
- @types/react override removal when DefinitelyTyped fixes upstream
- dotenv-cli test wrapper → shared test bootstrap
- HttpOnly cookie session migration
- Sentry/PostHog dashboard verification once accounts set up

The "tests pass, runtime fails" pattern hit 9 times across Phase 0.
Lesson banked permanently: every toolchain layer has its own
resolution model; manual/CI verification before "done" is
non-negotiable.

## Next: Phase 1 planning
(see ARCHITECTURE.md roadmap — first business module)

## PHASE 0 CLOSED — all 9 acceptance criteria met

Slice 9 (Playwright E2E + CI) merged. Both CI jobs (ci + e2e) green
and now required on main.

Phase 0 took 6 working days: scaffold → RLS multi-tenancy → auth →
onboarding → invitations → observability → CI → E2E.

Bugs surfaced by E2E, logged for pre-pilot fix:
- Zod .optional() ordering bug in branding + invite forms (empty
  strings rejected before .optional() rescues) — onboarding funnel leak

Carried forward (deferred.md): Windows Turbo crash (highest-leverage
post-Phase-0 fix), @types/react override removal, dotenv-cli →
shared test bootstrap, HttpOnly cookie sessions, Sentry/PostHog
dashboard verification.

"Tests pass, runtime fails" hit 9 times across Phase 0. Lesson banked:
every toolchain layer has its own resolution model; CI/manual
verification before "done" is non-negotiable.

Next: Phase 1 planning — first business module.

## Phase 1 scope LOCKED (planning only, no code)

Decisions made tonight:
- Build order: academic skeleton → students → enrollment → staff
- CSV import: robust (column mapping, per-row validation, partial
  success, error report); no AI dedup (Phase 5)
- Admission application workflow: deferred
- Guardians: captured in Phase 1 (data + CSV); parent auth → Phase 4
- ClassLevel: HYBRID (seed standard Nigerian levels per-school at
  creation, schools can edit/add)
- AcademicYear / Term: SEPARATE tables (year has many terms)
- Two thin AI-foundation tables (mastery, AI-interaction-log) folded
  into academic-structure schema; shape owned by Phase 5; empty until
  then. See deferred.md.

Strategy items captured to deferred.md for later market-verified
decisions: CBT, predictive AI, agentic positioning, Phase-7 modules.

## Tomorrow's first task
Prompt Claude Code: "Read ARCHITECTURE.md §6.2–6.5, CLAUDE.md, the
Phase 1 decisions in docs/journal/2026-05-19.md and deferred.md, then
draft docs/modules/phase-1.md" — full module spec: entities, Prisma
models (incl. the 2 thin AI tables), RLS policies, API endpoints, CSV
import design, UI screens, RBAC, acceptance criteria, slice breakdown.
Review the spec before any code.