# 2026-07-19 — Slice 3 follow-up confirmed; Slice 5 (parent-initiated Paystack payments) planned and built

Branch: `phase-4/slice-5-parent-payments` (implementation complete, not yet committed/pushed — reporting back for review first, same rhythm as Slices 3/4)

---

## Work log

### Slice 3 error-handling consistency — re-confirmed against actual `main`, not memory

Picked up an open thread from yesterday: whether `students/[id]/page.tsx`'s generic
"couldn't find that child" treatment is genuinely consistent across both its
failure points (`GET /portal/students/:id` and its `.../invoices` sibling),
not just claimed to be. Pulled the real file off `main` via `git show
origin/main:...` rather than trusting the merge report, and traced the logic
line by line: both 403/404 checks are combined into one condition, both
resolve to the same `{ kind: "not-found" }` state, same fixed copy, same
inline (non-redirect) rendering. Confirmed clean — no follow-up needed. Flagged
for the record that yesterday's merge report should have restated this
explicitly instead of just linking the PR; noted for next time.

### Slice 5 plan-first — parent-initiated Paystack payments (D4)

Given the "moves money, not just reads data" stakes, read the actual Phase 3
payment code before proposing anything: `PaymentsService.initPaystack`,
`handleWebhook`, `applyPaystackSuccess`/`Failed`, `verifyPaystack`, and
`PaystackController`. Key findings that shaped the plan:

- The webhook path is already 100% identity-agnostic — confirmed by reading
  it, not just trusting D4's own claim that no new webhook path is needed.
  It resolves everything from the `PSK-{schoolId}-{paymentId}` reference
  string; no `AuthContext` anywhere in that path.
- `recordedBy` on `Payment` is documented as "userId" specifically — not
  reusable for a guardian id. Resolved by writing `recordedBy: null` for
  guardian-initiated payments (no staff actor, same semantic the webhook
  path already uses) and recording *which* guardian in the audit log's
  `metadata` instead, mirroring `portal-auth.service.ts`'s own
  guardian-id-in-`audit_logs.user_id` precedent.
- The staff `initPaystack` call passes no `callbackUrl`, relying on
  Paystack's dashboard default (which points at `apps/web`) — wrong app
  entirely for a guardian. Guardian flow passes one explicitly, built from
  the existing `PORTAL_BASE_URL` env var.
- Checked `.env`'s `PAYSTACK_SECRET_KEY` — confirmed it's still the
  unfilled `.env.example` placeholder (`sk_test_replace_me`, byte-length
  match), not a real sandbox key. Non-issue: Phase 3's own
  `payments.service.spec.ts` already established the stub-at-consumer
  pattern for exactly this reason.
- Resolved "amount": server-computed as the exact outstanding balance,
  never accepted from the client — a deliberate divergence from the
  staff endpoint (which legitimately supports partial payments), making
  "you can only pay what's actually owed" a server-enforced invariant
  rather than a UI default.
- Found and flagged a real, pre-existing gap while reasoning through
  idempotency: neither `initPaystack` call site guards against a second
  in-flight PENDING payment on the same invoice, so two completed
  checkouts (double-click, two tabs) can both apply via the webhook and
  sum to a real overpayment. Proposed a narrow mitigation scoped to just
  the new guardian endpoint, explicitly not a fix to the shared webhook
  code — approved, with the instruction to also log it properly in
  `docs/deferred.md` once built (see below).

Plan approved as written.

### Slice 5 build

- `packages/types/src/portal-payments/`: `PortalPaymentDto` — drops
  `schoolId`, `recordedBy`, `paystackReference`, `reference`,
  `receiptNumber`/`receiptUrl` relative to the admin `PaymentDto` (receipts
  are an explicit non-goal this pass, logged in deferred.md alongside the
  overpayment gap rather than silently added).
- `apps/api/src/modules/portal-payments/`: `PortalPaymentsController`/
  `Service`. `POST /portal/students/:id/invoices/:invoiceId/pay` (no
  request body — amount is always server-computed) and
  `GET /portal/payments/:reference`.
- **Design pivot during implementation, not in the original plan**: the
  plan-first sketched the read endpoint as a full "verify against Paystack
  + apply the result" self-heal, mirroring the staff endpoint. Actually
  building it surfaced a real choice — `applyPaystackSuccess`/`Failed` are
  private methods on `PaymentsService`, so reusing them meant either a
  visibility change to Phase 3's most heavily-tested money code, or
  duplicating their receipt-generation/installment-recompute/audit-log
  logic (a much larger, riskier duplication surface than the ~3-line
  overpayment guard the *init* path duplicates). Neither was clearly right,
  so reconsidered the actual UX need: the webhook already applies success/
  failure asynchronously and independently: the callback page doesn't need
  to *trigger* that, just observe it. Landed on a **pure read** — no
  Paystack API call, no apply logic — with the frontend polling every 2s
  for ~20s. Solves the same problem (post-checkout redirect needs to show
  a result) with zero touches to Phase 3 finance code. Flagged this
  deviation explicitly rather than silently implementing something
  different from what was approved.
- In-flight guard: rejects a second `pay` attempt while a PENDING Paystack
  payment on the same invoice was created within the last 30 minutes.
  The 30-minute bound itself was a mid-implementation catch, not in the
  plan: an unbounded "any existing PENDING row blocks a retry" guard would
  permanently lock out a guardian who abandoned an earlier checkout,
  since nothing ever transitions an abandoned (not declined) PENDING row
  to FAILED.
- `apps/portal`: `InvoiceCard` (extracted from the Slice 4 invoice list)
  gained a "Pay ₦X" button — no amount field, matches the server-computed-
  balance design. New `payments/callback/page.tsx` polls the read endpoint
  and shows success/failure/still-confirming states.
- Tests: 13 in `portal-payments.controller.spec.ts` (real HTTP through
  `GuardianAuthGuard`) covering the happy path with kobo-exact amount
  assertion, `INVOICE_ALREADY_PAID`, the in-flight guard, same-school
  cross-guardian block, cross-tenant block both directions, 401, a
  Paystack-API-failure case, `getByReference`'s own negative walk, exact
  response-shape assertion, and — the one this slice's plan-first said
  mattered most — a duplicate `charge.success` webhook replay against a
  guardian-initiated payment applying once, using the real (unmodified)
  `PaymentsService.handleWebhook`, not a mock of it.
- Caught one test-fixture bug by actually running the suite, not just
  typechecking it: `createGuardianSession` opens its own `withTenant`
  transaction internally, and nesting a call to it inside an
  already-open outer `withTenant` block meant the inner transaction never
  saw the outer one's `app.current_school_id` GUC — FORCE RLS correctly
  rejected the `guardian_sessions` insert. Fixed by moving the session
  creation to run after the outer transaction closes.
- `docs/deferred.md`: logged the double-PENDING-row gap in full — what it
  is, that it's pre-existing (Phase 3, not introduced here), what Slice 5's
  narrow mitigation does and doesn't cover, and the two real-fix options
  for whenever it's prioritized.
- `audit-coverage.spec.ts`: new `payment.guardian-init` block, confirming
  the audit row's `userId` is the guardian id (no FK, same precedent as
  guardian login/accept) and `metadata` carries `invoiceId`/`amount`.

### Verification

`pnpm typecheck`/`lint` clean across `packages/types`, `apps/api`,
`apps/portal`. `apps/portal` production build succeeds, both new/changed
routes (`students/[id]`, `payments/callback`) compile. New specs: 13/13
(`portal-payments.controller.spec.ts`) + 25/25 (`audit-coverage.spec.ts`,
including the new guardian-payment block), all passing.

### Self-heal gap follow-up — pure read reversed, active-verify shipped

Before signing off, checked the two real questions the pure-read design's
own comment had assumed away rather than actually verified: what the
callback page shows once its poll window elapses, and whether staff have
any indirect way to unstick a guardian-initiated PENDING payment. Traced
both against the real code (not memory): the callback page's `PENDING`
branch silently stops polling with no actionable path forward, and
`FinanceService`/the debtor list have zero `PENDING`-status awareness — a
bursar can't see or manually resolve a stuck guardian payment through any
existing staff surface either. Neither held up as a real self-heal path,
so reversed the pure-read design per approved §3:

- `PaymentsService.verifyAndApply(schoolId, reference)` — new narrow
  public method, extracted from the old `verifyPaystack` (which is now a
  thin wrapper delegating to it). Both the staff endpoint and the new
  guardian endpoint call the same code, so there's exactly one "check
  Paystack, apply if not already applied" implementation, not two.
- `applyPaystackSuccess`/`applyPaystackFailed` hardened with an atomic
  `updateMany({ where: { id, status: "PENDING" } })` claim before doing
  any receipt/recompute/audit work — the loser of a concurrent race gets
  `count: 0` and bails before touching anything. This closes a real gap:
  `verifyAndApply` now has two independent callers (guardian poll,
  webhook) that can land concurrently, which the original single-caller
  design never had to defend against.
- `PortalPaymentsService.getByReference` (pure read) replaced with
  `verify()` — authorization (tenant + `withGuardian`) runs *before* any
  Paystack call, then delegates to `verifyAndApply`. A guardian polling a
  reference they have no business seeing never reaches Paystack.
- Frontend: `payments/callback/page.tsx` polls the same endpoint, but the
  semantics underneath changed materially — every poll is now a genuine
  Paystack check, so a response is always terminal (SUCCESS/FAILED) once
  it succeeds. There is no "still PENDING" response anymore, which made
  the old 20s/10-poll "still confirming" design dead code for its
  original purpose. Reworked: retries now back off *transient failures*
  (our fetch failing, or Paystack's API erroring) rather than an async
  webhook that might still land, 3s/5-attempt window (~15s), and a
  "Check again" manual retry button once that's exhausted — each click is
  a real independent verification attempt, not a hopeful re-read.
- New tests proving (not asserting) the concurrency claim: two
  simultaneous guardian polls of the same reference, and a guardian poll
  racing a direct webhook call for the same payment — both assert exactly
  one `payment.paystack-confirm` audit row and a non-doubled
  `invoice.totalPaid` afterward.

Full re-verification: `apps/api` typecheck/lint clean, full vitest suite
(73 files / 1280 tests, 2 intentionally skipped) green including the two
new concurrency specs; `apps/portal` typecheck/lint/build clean;
`packages/types` build clean; `apps/web` typecheck clean (sanity only,
untouched by this change).

---

## Next

- Awaiting review before this branch is committed/pushed/PR'd — same
  gate as Slices 3 and 4, given the explicit "treat with the same rigor
  as Phase 3" instruction this slice carried.
- The double-PENDING-row gap is logged, not fixed at the root (webhook-
  apply-time re-validation) — deliberately, per the approved plan's scope
  boundary. Worth someone's eyes before a pilot school does real volume.
  Unrelated to and not resolved by today's concurrency fix, which closes
  the *single-row* concurrent-apply race, not the *multiple-row*
  overpayment gap.
- No receipt view for guardian-facing payments yet (also just logged in
  deferred.md) — the invoice detail page's own totalPaid/status refresh is
  the only confirmation surface right now.
- Phase 4 Slices 1, 2, 3, 4, 6 are live on `main`; Slice 5 is the last one
  before the phase-close slice (8 in the doc's table — admin `@Permissions`
  for the newer staff actions, audit coverage extension, cross-tenant +
  cross-family negative-walk E2E, manual gates).
