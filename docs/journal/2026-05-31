# 2026-05-31

## Starting state
Slice 10 closed yesterday (PRs #24/#25/#26). Phase 1 at 10 of 13 slices.
On main, tree clean. Slice 10 shipped TeacherProfile + class-teacher
consumption + teacher CSV import + staff UI — the groundwork. It did NOT
close deliverable-18 / acceptance #9 (teacher sees only assigned); that
is this slice.

## Today's outcome
Slice 11 — TeacherAssignment + teacher portal + the service-layer scope
filter that closes acceptance #9. Plan-first because the assignment shape,
the scope-filter mechanism, and the portal minimum all warranted settling
before code.

## Plan (settled before code; recommendations confirmed by user)

### Q1 — TeacherAssignment shape (spec-locked, phase-1.md:180-204)
`teacher × classArm × subject × academicYear × (optional term)`. Direct
FKs to classArm + subject — NOT through ClassSubject (ClassSubject is
classLevel×subject; a teacher teaches a specific arm, not a level).
Unique `(schoolId, teacherId, classArmId, subjectId, academicYearId,
termId)`, `termId` nullable (null = whole year, set = term-specific).
Closest to candidate (c), plus year scoping + optional per-term override.

### Q2 — cardinality
Teacher→many assignments: yes. **Co-teaching ALLOWED** (no app guard):
`teacherId` is inside the unique key, so two teachers can share an
(arm, subject, year, term). Matches the schema; supports lab/team
teaching. Tightening later is a cheap app-rule; loosening is a migration.
- NOTE: the DB unique treats NULL term as DISTINCT (Postgres), so two
  identical whole-year (termId=NULL) rows would NOT collide at the DB.
  The service adds a pre-check (match incl. null term) returning a clean
  409 TEACHER_ALREADY_ASSIGNED, with the P2002 catch as a backstop for the
  non-null race. The "duplicate" spec case asserts this.

### Q3 — scope filter (lands cp2; reconciliation noted here for when it does)
Lives in the SERVICE layer (needs DB access to teacher_assignments; it's
in-school authorization, not tenancy; PermissionsGuard isn't wired until
slice 13). **Spec drift to reconcile in cp2:** phase-1.md:1150's example
`user.roles.includes('teacher')` cannot work as written — AuthContext is
role-free by design (auth-context.ts; roles are re-fetched, see
role-check.ts). cp2's `getTeacherScope` re-fetches roleKeys the same way
`assertUserActiveAndHasOneOf` does. **Record this drift in the cp2 journal
entry when cp2 lands.**

### Q3a — teacher endpoint surface (confirmed: dedicated)
Teachers hit ONLY a small set of dedicated `/teacher-*` scope-filtered
read endpoints (cp2). All admin CRUD stays owner|admin and rejects
teachers outright — smallest security surface (phase-1.md:1265 risk note).

### Q4 — teacher portal minimum (cp3)
- /teacher/dashboard — "Welcome. You are assigned to: <arms + subjects>."
- /teacher/classes — arms in scope (subject-assignment ∪ homeroom).
- /teacher/classes/[armId] — roster + this teacher's subjects; 404 if
  armId ∉ scope (the testable security property).
- /teacher/profile — already shipped slice 10; untouched.
No timetable/grading/announcements (Phase 2/4).

## Checkpoints (3; cp3 may split to 4 if UI runs long)
- **cp1 (this session)** — TeacherAssignment model + 5 back-relations +
  migration + RLS policy + RLS-spec coverage + admin CRUD module + DTOs +
  PHASE_1_SLICE_11_PERMISSIONS + service spec. API only, no scope yet.
- cp2 — getTeacherScope helper + role re-fetch + dedicated scoped teacher
  read endpoints + security tests (teacher→other arm 404, teacher→admin
  CRUD 403). The Q3 drift reconciliation is recorded here.
- cp3 — admin assignment UI on /staff/[userId] (closes slice-10's
  deferred "assigned subjects") + teacher portal pages + nav + Playwright
  E2E (admin assigns → teacher sees only assigned). cp3 carries forward
  slice-10 cp3 form discipline: zero `as never` casts, local
  FormValues-matched schemas, root + per-field error blocks. Does NOT
  retroactively audit the older academic dialogs (own deferred entry).

## Grounding notes (verified against repo)
- The slice-10 `teacher` system-role seed ALREADY grants the full slice-11
  read scope (teacher-assignment.read, student.read, enrollment.read,
  class-arm.read, …) — no seed migration needed this slice.
- Permissions follow the per-slice reference-only pattern; ALL_PERMISSIONS
  stays Phase-0-only until slice 13's rollup.
- SECURITY DEFINER count stays 4 — every endpoint is post-tenant.

## Acceptance
This slice closes acceptance #9 + deliverable-checklist 18-19. The
class-teacher half (classArm.classTeacherId, slice 10) folds into
getTeacherScope via the homeroom-arms union (cp2). Phase 1 → 11/13.

## cp1 progress (API foundation — landed, awaiting review; nothing committed)
Built:
- schema.prisma — TeacherAssignment model + 5 back-relations (User via
  named `TeacherSubjectAssignments`, ClassArm, Subject, AcademicYear, Term).
  Fixed the stale User comment that attributed this edge to slice 10.
- migration 20260601000000_phase_1_slice_11_teacher_assignments — table +
  4 indexes + unique(school_id,teacher_id,class_arm_id,subject_id,
  academic_year_id,term_id) + 5 FKs (term_id SET NULL, rest CASCADE) + RLS
  ENABLE/FORCE/tenant_isolation. Generated via migrate diff, applied via
  migrate deploy.
- policies/phase-1.sql — teacher_assignments RLS block appended (source of
  truth, kept in sync with the migration).
- rls.spec.ts — slice-11 describe block (5 tests: A/B isolation, x-tenant
  findUnique null, WITH CHECK reject, unset-GUC zero rows).
- packages/types/src/teacher-assignments/ — Dto + create/update/list Zod
  DTOs + barrel; registered in types index. PHASE_1_SLICE_11_PERMISSIONS
  added (reference-only; teacher role already has teacher-assignment.read).
- apps/api modules/teacher-assignments/ — controller (owner|admin via
  AuthGuard + service gate), service (CRUD + FK/teacher-role validation +
  TERM_YEAR_MISMATCH + duplicate pre-check incl. NULL-term gap + P2002
  backstop + audit), module; registered in app.module.
- teacher-assignments.service.spec.ts — 15 integration tests.

Decisions confirmed in code:
- Duplicate guard pre-checks active rows incl. null term (DB unique misses
  NULL-term dup); co-teaching (different teacher, same tuple) ALLOWED, with
  a test; re-assign-after-deactivate allowed, with a test.

Verification (all green; nothing pushed):
- migrate deploy: applied cleanly.
- typecheck: types + api clean.
- lint: api `eslint . --max-warnings=0` clean (covers the basePrisma rule);
  no basePrisma in service/controller.
- test: **419 passed (30 files)** = prior 399 + 20 (15 service + 5 rls),
  +1 file. No regressions — slice-3 class-arms/class-subjects and slice-10
  ClassTeacherArms specs still green alongside the new TeacherSubject edge.
- SECURITY DEFINER count: still 4 (slice-11 migration adds none).

## cp2 progress (teacher scope filter — landed, awaiting review; nothing committed)

### Q3 spec-drift reconciliation (RESOLVED in code)
phase-1.md:1150 shows the scope check as
`if (user.roles.includes('teacher')) { ... }`. **That cannot work in this
codebase** — `AuthContext` (apps/api/.../auth/auth-context.ts) is role-free
by design: it carries only `sessionId`, `userId`, `schoolId`, with a header
comment "Roles and permissions are NOT included — handlers that need them
re-fetch via withTenant" (the no-stale-permissions rationale). So cp2 does
NOT read roles off the JWT/AuthContext. Instead it re-fetches the user +
role grants under withTenant via the established
`assertUserActiveAndHasOneOf(authCtx, ["teacher"])` (apps/api/.../auth/
role-check.ts) — the exact pattern every admin endpoint already uses, just
gated on `teacher` instead of `owner|admin`. This is the one spec-vs-code
divergence flagged in the cp1 plan; it is now reconciled and tested
(getMyScope as admin → 403 proves roles come from the DB, not the token).

### Built
- packages/types/src/teacher-scope/ — TeacherScopeDto, ENRICHED (post-review
  extension): `classArms: [{id,name,code}]` +
  `subjectsByArm: Record<armId, [{id,name,code}]>` (TeacherScopeArmDto /
  TeacherScopeSubjectDto), plus a TRIMMED TeacherRosterStudentDto +
  TeacherRosterResponse. Registered in the types barrel.
- apps/api modules/teacher-scope/teacher-scope.helper.ts — `getTeacherScope(
  db, teacherId, year?)`: subject-assignment arms (active only, optional
  year filter) ∪ homeroom arms (ClassArm.classTeacherId, active). Two
  queries, unioned in code — no N+1. Each arm/subject carries {id,name,code}
  pulled via a select-widening on the SAME two queries (no extra round-
  trips). Pure over (db, teacherId, year?); caller owns the withTenant
  boundary. subjectsByArm stays a Map in the helper; the service converts to
  the wire Record (the Map-in-helper / Record-on-wire split cp2 already used).
- teacher-scope.service.ts — getMyScope + getMyArmRoster, both gated
  `["teacher"]` via the role re-fetch above. Roster returns current-term
  students of ONE in-scope arm; out-of-scope/cross-tenant arm → 404 (not
  403) so the arm reads as non-existent, matching RLS semantics.
- teacher-scope.controller.ts — GET /teacher-scope/me and
  GET /teacher-scope/me/arms/:armId/students. Thin pass-through; no admin
  branch (Q3a — dedicated teacher surface).
- module registered in app.module.

### Security test matrix (teacher-scope.service.spec.ts — all 12 green)
1. subject-assignments only → those arms + subjectIdsByArm populated ✓
2. homeroom only → that arm, NO subjectIdsByArm entry ✓
3. subject + homeroom → union, deduped (overlapping arm once) ✓
4. neither → empty arrays (not an error) ✓
5. year filter restricts subject assignments ✓
6. cross-tenant → empty (RLS hides other school's assignments/homeroom) ✓
7. GET /me as teacher → 200 with own scope ✓
8. GET /me as admin → 403 (ForbiddenError) ✓
9. roster in-scope arm → 200 with students (+ asserts no PII fields) ✓
10. roster out-of-scope arm (same tenant) → 404 ✓
11. roster arm in another tenant → 404 ✓
12. teacher hitting admin CRUD (POST /teacher-assignments) → 403 ✓
   (plus a 13th: unknown arm id → 404)

### Divergence (flagged for review)
- **Roster PII-minimisation (KEPT — confirmed right call).** The roster
  returns a TRIMMED student shape (id, admissionNumber, name, gender,
  photoUrl, status) — NOT the full StudentDto. A class register doesn't need
  medical notes / address / contact / DOB. Separate concern from the scope
  enrichment below; student PII stays narrow.
- **/me is not year-scoped (KEPT — confirmed right call).** getMyScope
  returns all active assignments across years (helper supports a year
  filter; the endpoint passes none). cp3 can pass a year for a single-year
  view.

### Post-review extension (resolves cp2 divergence 3 — Option A)
Enriched GET /teacher-scope/me with arm + subject NAMES + CODES so cp3
renders from ONE round-trip instead of three:
- Helper `TeacherScope` + `TeacherScopeDto`: `classArmIds` →
  `classArms: [{id,name,code}]`; `subjectIdsByArm` →
  `subjectsByArm: Record<armId,[{id,name,code}]>`.
- Implementation: widened the two existing selects (ClassArm + Subject
  {id,name,code}); NO new queries — same N+1-free shape.
- Roster endpoint UNCHANGED (already returns student names).
- PII check: arm/subject names + codes are class-level identifiers every
  admin endpoint exposes — not student data. Roster trim (test #9) untouched.
- Tests: cases 1-6 (helper) + #7 (/me endpoint) now assert the richer shape
  (ids + names + codes + structure). No new tests — same 13 in the file.

### Verification (all green; nothing pushed) — incl. post-review enrichment
- typecheck: types + api clean.
- lint: api `eslint . --max-warnings=0` clean; no basePrisma in helper/
  service/controller (withTenant only).
- test: **432 passed (31 files)** = prior 419 + 13 (12 matrix + 1 bonus
  unknown-arm-id 404), +1 file. Enrichment changed assertions only — count
  unchanged at 432. No regressions.
- SECURITY DEFINER count: still 4 — cp2 is app-layer only, zero SQL/
  migration changes.

## cp3 progress (teacher portal UI — landed A/B/C, awaiting review; nothing committed)

Web-only slice consuming cp1 (admin CRUD) + cp2 (scope reads). NO API/types
changes (git status: only apps/web touched), so the API suite is structurally
untouched at 432.

### A — Admin assignment UI (closes slice-10 "assigned subjects" deferral)
- components/staff/teacher-assignments-section.tsx — lists the teacher's
  TeacherAssignment rows (arm · subject · year · term-or-"Whole year"),
  inline Add form (4 native selects: arm / subject / year[default current] /
  term[default Whole year, filtered to the selected year's terms]), per-row
  hard delete (DELETE /teacher-assignments/:id). Resolves ids→names via
  listClassArms(includeInactive) / listSubjects(includeInactive) /
  listAcademicYears / listTerms(per year).
- Wired into (admin)/staff/[userId]/page.tsx after the class-teacher section.
- FORM-CLASS PROTECTION (slice-10 lesson): local `assignmentFormSchema` ===
  FormValues (4 strings) → ZERO `as never`; root error block + per-field
  errors on all four; API codes mapped — TEACHER_ALREADY_ASSIGNED → root,
  TERM_YEAR_MISMATCH → termId, INACTIVE_CLASS_ARM → classArmId,
  INACTIVE_SUBJECT → subjectId, else → root; termId "" = whole year (→ null).

### B — Teacher portal pages (route group VERIFIED)
Pages land at (teacher)/teacher/<segment>/page.tsx (NOT (teacher)/<segment>/),
per the layout's ROUTING GUARD:
- /teacher/dashboard — welcome (useAuth firstName) + grouped classArms with
  their subjects; empty state when no scope.
- /teacher/classes — list of in-scope arms → links to [armId].
- /teacher/classes/[armId] — arm name + this teacher's subjects + PII-narrow
  roster (avatar, name, admission #, status). 404 from the roster endpoint →
  "not one of your classes" state (the acceptance-#9 security property).
All read ONLY /teacher-scope/me + /teacher-scope/me/arms/:armId/students.

### C — Teacher nav
- components/teacher/sidebar.tsx — minimal TeacherSidebar (Dashboard /
  Classes / Profile only; NOT the admin sidebar). (teacher)/layout.tsx now
  uses the same sidebar+topbar shell as (admin) but with the teacher nav.

### API client wrappers
- lib/staff/staff-api.ts — listTeacherAssignments / createTeacherAssignment /
  deleteTeacherAssignment.
- lib/teacher/teacher-scope-api.ts — getMyScope / getMyArmRoster.

### D — Playwright E2E → SPLIT to cp4 (the locked plan reserves this)
Not built today. Rationale: the e2e harness (e2e/tests/phase-0-happy-path
.spec.ts) has NO teacher-login or academic-structure-setup helper — the only
flow is a 231-line signup→onboard→invite→accept→login. The acceptance-#9 E2E
needs the FULL chain (owner signs up → creates year+term+level+arm+subject via
the settings UI → invites a teacher → teacher accepts → owner adds a
TeacherAssignment via the new /staff/[userId] UI → teacher logs in → asserts
dashboard + the cross-teacher 404). That fixture is hours of fragile setup;
the prompt's SCOPE GUARD explicitly says don't bundle E2E under deadline
pressure. cp4 owns: the two E2E tests + a reusable academic-structure /
teacher-login harness (which slice 13 will also extend).

### Divergence (forced — flagged)
- **Teacher pages are CLIENT components, not server components.** The prompt
  said "server component, reads /teacher-scope/me on load" + server
  notFound(). The whole app authenticates via a Bearer token in localStorage
  (api-client.ts), which a server component can't read — every existing page
  (incl. (teacher)/teacher/profile) is "use client". So the pages are client
  components fetching via apiFetch, and the [armId] out-of-scope case renders
  an in-page "not one of your classes" state instead of server notFound().
  Same UX, same security (the 404 still comes from the server).

### Verification (nothing pushed)
- web typecheck: clean. web lint `eslint . --max-warnings=0`: clean.
- ZERO `as never` casts (grep: only comment mentions).
- route group confirmed: (teacher)/teacher/{dashboard,classes,classes/[armId]}.
- API suite: structurally untouched (web-only diff) — stays **432**.
- **Manual browser walkthrough is the remaining gate** (admin assign →
  teacher portal end-to-end) + cp4 E2E. Handing back for the walkthrough.

## cp4 progress (acceptance-#9 E2E — landed, awaiting review; nothing committed)

E2E-only slice. git status: only `e2e/` touched (+ `pnpm-lock.yaml`), so the
API suite is structurally untouched — stays **432**. cp4 adds TWO tests to the
separate Playwright suite (now 3 total: the Phase-0 happy path + these two).

### PART A — reusable fixture harness (e2e/fixtures/)
Built so slice 13's acceptance #11 rollup reuses it. Design principle:
**API-FIRST SETUP, UI-ONLY ASSERTIONS** — academic structure, invitations and
assignments are built over HTTP / the tenant client; the browser is reserved
for asserting the teacher portal.
- `api.ts` — typed wrappers over Playwright's APIRequestContext for signup,
  onboarding (1–5 → ACTIVE), academic CRUD (year/term/level/subject/arm,
  incl. set-current), `/auth/me`, and `POST /teacher-assignments`. PATH NOTE:
  baseURL carries a trailing slash and paths are prefix-relative — Playwright
  resolves `new URL(path, baseURL)`, so a leading slash would drop `/api/v1`.
- `db.ts` — `seedTeacherInvitation()` writes a `roleKey='teacher'` Invitation
  row through `withTenant` (the documented divergence — see below).
- `session.ts` — `loginAsAdmin` (provisions a fresh ACTIVE school; owner ⊇
  admin for setup) and `loginAsTeacher` (API login + token injection). Both
  return browser contexts with the bearer pre-injected into localStorage
  (`sk_auth_token`) via `addInitScript`, so the app hydrates via `/auth/me`
  with no UI-login round-trip.
- `academic.ts` — `setupAcademicStructure` (reuses the signup-seeded JSS 2
  level; creates year+term [current] + subject + arms; find-or-create when
  `skipIfExists`), `assignTeacher`, `armId`.
- `teacher.ts` — `inviteAndAcceptTeacher`: seeds the invitation, then ACCEPTS
  it through the REAL public UI in a fresh context. userId is read race-free
  via `/auth/me` against the freshly-stored token (NOT the accept response
  body — the form hard-navigates to /dashboard, which discards it).
- `unique.ts` — per-run unique suffixes + globally-unique owner phone.

### PART B — the two tests (e2e/tests/slice-11-teacher-scope.spec.ts)
1. **assign → sees** — admin assigns a teacher to JSS 2 A · Mathematics; the
   teacher's /teacher/dashboard shows "JSS 2 A" + "Mathematics", /teacher/
   classes lists it, the arm page shows the heading + subject + empty-roster
   state.
2. **cross-teacher isolation** — A→JSS 2 A, B→JSS 2 B. A's /teacher/classes
   shows only JSS 2 A (asserts `toHaveCount(0)` for "JSS 2 B" AFTER A's arm has
   rendered); A navigating to B's arm URL hits the in-page "not one of yours"
   state (not a 404 page, not a crash); symmetric for B.

### Divergence (the big one — flagged, sanctioned by the cp4 brief)
- **No API mints a teacher invitation.** The brief's "POST /users/invite with
  roleKey='teacher'" doesn't exist in that shape: in Phase 0 that endpoint is
  admin-only and HARDCODES `roleKey:"admin"` (users.service.ts). The only
  teacher-invite paths today are the async bulk-CSV import (BullMQ worker — too
  heavy/flaky for an E2E setup step) and the not-yet-built slice-13 RBAC. The
  brief explicitly sanctions "fetch the invitation directly from the DB", so
  `seedTeacherInvitation` inserts the row via `withTenant` (same INSERT policy
  the real `UsersService.invite` uses) and the REAL accept flow + REAL login do
  everything else — the accept handler is role-generic (grants whatever
  `role_key` the row carries), so this yields a genuine teacher: real password,
  real session, real role grant. The ONLY synthetic step is the row insert;
  when a teacher-invite API lands in slice 13, swap that one function for it —
  the accept half is already production-faithful. (This is why `e2e` now
  depends on `@school-kit/db`, and `playwright.config.ts` loads the root `.env`
  before any worker imports the tenant client.)
- **Setup is API-first, NOT settings-UI-driven.** The cp3 journal pictured cp4
  building academic structure "via the settings UI". cp4 uses the admin API
  instead (the brief's API-FIRST principle): faster, far less flake surface,
  and the UI for that structure is already covered by slice-1–3 specs. The
  browser is used ONLY for the teacher portal under test.

### Flake-prevention disciplines applied
- One `browser.newContext()` per session (admin + each teacher); contexts
  closed in a `finally`.
- Specific DOM waits only (`getByText`/`getByRole`/`waitForURL`) — ZERO
  `waitForTimeout`.
- Fresh, uniquely-identified school per test (unique slug/email/phone) — no
  shared state, re-runs don't collide.
- "Absent" assertions gated behind a positive render first (assert JSS 2 A
  visible, THEN JSS 2 B count 0) so they can't pass on a not-yet-loaded page.

### Verification (nothing pushed)
- e2e `tsc --noEmit`: clean (added a `typecheck` script to the e2e package).
- New spec: **3 consecutive runs, both tests green each = 6/6** (no flake).
- Headed run: green. Full e2e suite (happy path + 2 new): **3 passed**.
- API suite: untouched (e2e-only diff) — stays **432**.

## Slice 11 — CLOSED
Acceptance #9 (acceptance-bar step 8: "each teacher logs in and sees exactly
and only the arms/subjects they teach") is now codified end-to-end. cp1
(model+RLS+admin CRUD) + cp2 (scope filter + scoped reads) + cp3 (portal UI) +
cp4 (E2E) complete. Deliverables 18–19 done. **Phase 1 → 11/13.**

## Tomorrow's first task
- Slice 12 — AI foundation tables (`MasteryRecord` + `AIInteractionLog`):
  models, migration, RLS, extend the RLS isolation spec. Small slice; land it
  before slice 13's RBAC/audit/E2E close-out.
