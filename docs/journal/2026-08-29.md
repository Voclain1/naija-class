# 2026-08-29 — mobile session-end UX

`fix/mobile-session-end-ux` changes only the shared React Native session
primitive and its login presentation. Server 401 reasons are normalized once
in `apps/mobile/src/lib/api/client.ts`; `SessionProvider` clears the bearer and
protected query cache without re-calling logout, and passes a principal-scoped
one-time explanation to the login screen. Intentional guardian/student/staff
logout suppresses a raced 401 notification. `ApiNetworkError` remains
non-terminating.

Local evidence: workspace types build, mobile typecheck, mobile lint, and the
full mobile Vitest suite all pass (19 files, 163 tests). No Android SDK,
emulator, or device is available on this host, so guardian/student/staff native
runtime forced-401, normal logout, and offline checks remain required before
merge. PR CI may run in parallel. No iOS claim was made; the known staff iOS relock issue is
untouched.

## Post-merge disposition

PM explicitly waived physical Android runtime validation as a merge blocker
after fresh CI, focused mobile tests, and a successful development APK build.
Android runtime behavior was not verified. The required guardian/student/staff
terminating-401, deliberate-logout, network-failure, principal-isolation, and
one-time-notice lifecycle smoke is recorded in `docs/deferred.md` as a
post-merge physical-device check.

## Guardian/student consistency UX (in progress)

`fix/guardian-student-consistency-ux` is a presentation-only follow-up. It
centralises human invoice-status labels, replaces the student mobile raw enum,
adds explicit mobile result-card labels, gives the guardian portal a read-only
view of the already-existing released-results API, and adds known payment
context (amount, child and term) after a confirmed mobile guardian payment.
No payment reconciliation, report-release rule, backend API, schema, migration
or offline-write policy changed.

Focused local checks passed: types, mobile and portal typechecks; mobile and
portal lint; and 11 focused mobile tests. Disposable browser baseline setup
could not complete on this host: the isolated worktree has no safe local API
environment, and policy blocked inheriting the root `.env` because it may
contain non-local credentials. No browser or Android runtime result is claimed.

Follow-up: the branch was safely replayed from `1feaae9` onto `bfaf96d` with
`git rebase --onto origin/main 1feaae9`; main's onboarding-only change had no
semantic overlap. A disposable loopback-only Postgres/Redis harness at
`127.0.0.1:55432` / `127.0.0.1:56379` received all existing migrations, and
local API/web/portal configuration was explicitly localhost-only. This Codex
host terminates the spawned Node processes after startup, preventing the
focused browser regression from reaching an assertion. The new guardian
released-results Playwright regression typechecks, but is not claimed as run.
