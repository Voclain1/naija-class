# 2026-08-31 - Wave 4 shared error primitives (in progress)

## Scope

Work is isolated on `fix/shared-error-primitives` in
`C:\tmp\schoolkit-shared-error-primitives`, replayed onto `origin/main` at
`a9c4ad9` after PR #237. It is presentation-only: shared web error presentation, Finance
read-state truthfulness and focused regression coverage. No Finance backend,
payment reconciliation, mobile, report-card, onboarding, Prisma, migration or
offline-write behavior is changed.

## Baseline and implementation

- The repeated destructive error-banner shape appeared in 99 JSX instances
  across 79 web files. The central error token had 3.23:1 text contrast over
  the existing `bg-destructive/5` banner treatment.
- Finance had 15 fetch catches that converted a failure into an empty value or
  silence. The invoice-detail surface also contained nine raw `Error.message`
  render paths; these now use the reviewed Finance error-copy helper.
- `InlineAlert` is a small web-only `role="alert"` primitive with a concise
  title and optional recovery action. It is adopted by the migrated Finance
  screens and the admin dashboard. The light token now calculates to 6.98:1
  on the banner background; the dark token calculates to 6.68:1.
- Finance dashboard, debtors, discounts, fees, invoice detail, invoice list,
  expenses and payroll now distinguish a failed read from a genuine empty
  collection and keep previously loaded data where the page already has it.

## Validation and evidence boundary

- Web lint and typecheck pass.
- Focused Vitest: 4 files, 30 tests pass. E2E typecheck passes.
- `git diff --check` passes.
- A disposable Playwright alert/contrast/retry regression is added and
  typechecked, but it has not run locally: this worktree has no `.env` and no
  disposable Postgres/Redis services. Do not claim local browser or axe runtime
  evidence until the isolated harness or PR CI runs it.

