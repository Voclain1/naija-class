# 2026-08-30 — Wave 3B terminology and developer-language cleanup (in progress)

## Scope

Work is isolated on `fix/terminology-developer-language` from `6064ad6` in
`C:\tmp\schoolkit-guardian-student-ux`; the shared checkout remains untouched.
This is presentation-only: no API contract, domain enum, Prisma schema,
migration, authorization, payment reconciliation, attendance, session-end or
offline-write behavior changed.

## Implemented findings

- Enrollment list and carry-over candidates now use the established human
  student-identity fallback (`name`, then admission number, then plain
  “Unknown student”), never a truncated UUID. Bulk-create errors show the
  selected student's label rather than its identifier.
- Dashboard and import-mapping failures use fixed actionable copy instead of
  exception text; the guardian child-access fallback no longer appends a raw
  HTTP status.
- Shared financial status labels cover invoices, payments, payroll and
  Paystack setup requests. Enrollment status pills now use title-cased labels.
- User-facing product spelling is normalised to `SchoolKit` across the touched
  web, portal and mobile surfaces. URLs, package names, app scheme and Android
  package identifiers were intentionally left unchanged.

## Validation

- `pnpm --filter @school-kit/types typecheck` — pass.
- `pnpm --filter @school-kit/web typecheck` — pass.
- `pnpm --filter @school-kit/portal typecheck` — pass.
- `pnpm --filter @school-kit/mobile typecheck` — pass.
- Web and mobile lint — pass; portal lint was also run without errors.
- Focused web Vitest suite — 5 files, 13 tests passed.
- Full mobile Vitest executed all 19 listed files (164 assertions reported by
  the file counts); this host ended the process before its final summary, so
  it is not recorded as a clean command exit.
- `git diff --check` — pass.

## Evidence boundary

No disposable browser/API runtime result is claimed yet. The next owner should
use the disposable local harness or PR CI for browser-level verification; do
not treat the static checks as proof of rendered runtime behavior.

## PR CI evidence

PR #235's isolated GitHub Actions run passed: the combined lint/typecheck/test
and web/portal build job completed in 5m03s, and Playwright completed in 7m03s
with 67 passed and one retried flaky pre-existing guardian logout assertion.
The new `terminology-presentation.spec.ts` passed all three rendered cases:
enrollment human identity/title-cased status without UUID, dashboard recovery
copy without the intercepted backend text, and guardian portal fallback/brand
copy without a raw HTTP status. The mobile CI test process completed normally:
20 files and 165 tests passed. Web and portal previews deployed successfully.
