# 2026-09-04 - First-login tour sidebar refresh

## Scope

Small, approved update to the owner/admin first-login orientation tour:
add the current Insights navigation item, update two existing descriptions,
and keep the tour's intentionally single-page sidebar scope intact.

## Changes

- Added an **Insights** spotlight after **Report Cards**, matching the current
  enabled admin sidebar order.
- Kept **Dashboard** unspotlighted because the tour starts on that page, and
  corrected the implementation comment to describe that exception accurately.
- Updated the existing **Finance** copy to mention shareable payment links and
  the existing **Settings** copy to mention AI usage.
- Did not add a curriculum spotlight: curriculum upload belongs to the teacher
  navigation, outside this owner/admin sidebar tour.

## Validation and remaining gate

- `pnpm.cmd --filter @school-kit/web typecheck` and
  `pnpm.cmd --filter @school-kit/web lint` completed without errors.
- `git diff --check` passed.
- A real desktop/mobile browser replay remains pending. There was no local
  web/API runtime listening on ports 3001/4000 and this checkout has no `.env`
  for safely starting the authenticated E2E stack. The signed-in production
  Chrome tab remains useful for verifying the deployed version, but cannot
  validate this un-deployed local change. Do not describe the new Insights
  step as browser-verified until a released build or authenticated local test
  environment is available.
